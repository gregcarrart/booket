
<div class="body-content">
    <div class="container-fixed">
        <div class="project-title"><h1>{{headline}}</h1></div>
        <hr>
        <div class="project-meta">
            <div class="project-client">CLIENT: {{client}}</div>
            <div class="project-role">ROLE: {{role}}</div>
            <div class="project-url">URL: <a href="{{url}}" target="_blank">{{url}}</a></div>
            <div class="project-desc">{{{content}}}</div>
        </div>
    </div>
    <div class="project-images">
        {{#if embed}}
        <style>.embed-container { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; max-width: 100%; margin-bottom: 3.5%;} .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }</style>
        <div class='embed-container'>
            {{{embed}}}
        </div>
        {{/if}}
        {{#each postImages}}
        <div class="project-image"><img src="{{this}}" /></div>
        {{/each}}
    </div>
</div>